name: docs

on:
  push:
    branches:
    - main

jobs:
  docs:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Drop the tools mise.toml marks as local-only
        run: sed -i '/# \[skip ci\]/d' mise.toml
      - uses: jdx/mise-action@c2a87611a18de5b3828c5652fe268e992400cb5c # v4.3.0
      - run: mise run tbls
      # Pushing with GITHUB_TOKEN does not retrigger this workflow, so the
      # commit cannot loop back into another docs run.
      - name: Commit the regenerated schema section
        run: |
          if git diff --quiet -- README.md; then
            echo "README.md schema section is up to date"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "docs: update tbls schema section in README" -- README.md
          git push
      - run: uv sync --group docs --locked
      - run: mise run docs
      - uses: peaceiris/actions-gh-pages@84c30a85c19949d7eee79c4ff27748b70285e453 # v4.1.0
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./docs
